{
  "name": "campectus",
  "private": true,
  "description": "Racine du dépôt — le site Angular est dans frontend/",
  "scripts": {
    "setup": "npm install --prefix frontend",
    "start": "npm run start --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
